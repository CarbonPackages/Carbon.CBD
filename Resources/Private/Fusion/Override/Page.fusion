prototype(Neos.Neos:Page) {
    head.carbonCBD = Neos.Fusion:Tag {
        @if.onlyInBackend = ${renderingMode.isEdit}
        tagName = 'link'
        attributes {
            rel = 'stylesheet'
            href = ${StaticResource.uri('Carbon.CBD', 'Public/Plugin.css')}
        }
    }
}
